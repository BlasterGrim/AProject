@model EFGetStarted.AspNetCore.NewDb.Models.Acquisti

@{
    ViewData["Title"] = "Create";
}

<h2>Create</h2>

<h4>Acquisti</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="ClienteId" class="control-label"></label>
                <select asp-for="ClienteId" class ="form-control" asp-items="ViewBag.ClienteId"></select>
            </div>
            <div class="form-group">
                <label asp-for="SpedizioneId" class="control-label"></label>
                <select asp-for="SpedizioneId" class ="form-control" asp-items="ViewBag.SpedizioneId"></select>
            </div>
            <div class="form-group">
                <label asp-for="FatturaId" class="control-label"></label>
                <select asp-for="FatturaId" class ="form-control" asp-items="ViewBag.FatturaId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
